# Default text overlay elements.
# Copy to ./text_overlays.yaml in your project directory and customise.
#
# font:          path to a .ttf file used as the default for all elements (null = bundled Vera.ttf)
# content types: timecode | framecounter | framerange | date | time_of_day |
#                filename | sequence_name | metadata.<key> | <custom-cli-key>
# anchor:        top-left | top-center | top-right |
#                bottom-left | bottom-center | bottom-right | center
# default:       shown when the content resolves to nothing (e.g. --text key not passed)
# font_size and offset values are relative to 1080p and scale automatically.

font: Vera.ttf
enable: true

elements:

  - content: sequence_name
    anchor: top-left
    offset: [28, 28]
    font_size: 28
    color: [1.0, 1.0, 1.0, 1.0]
    shadow: true

  - content: date
    anchor: top-right
    offset: [28, 28]
    format: "%Y-%m-%d"
    font_size: 28
    color: [1.0, 1.0, 1.0, 1.0]
    shadow: true

  - content: time_of_day
    anchor: top-right
    offset: [28, 28]
    format: "%H:%M"
    font_size: 28
    color: [1.0, 1.0, 1.0, 1.0]
    shadow: true

  - content: timecode
    anchor: bottom-center
    offset: [0, 28]
    font_size: 28
    color: [1.0, 1.0, 1.0, 1.0]
    shadow: true

  - content: framecounter
    anchor: bottom-right
    offset: [28, 28]
    font_size: 28
    color: [1.0, 1.0, 1.0, 1.0]
    shadow: true

  - content: framerange
    anchor: bottom-right
    offset: [28, 28]
    font_size: 28
    color: [1.0, 1.0, 1.0, 1.0]
    shadow: true

  - content: user
    anchor: bottom-left
    offset: [28, 28]
    default: ""
    font_size: 28
    color: [1.0, 1.0, 1.0, 1.0]
    shadow: true

  - content: description
    anchor: bottom-left
    offset: [28, 28]
    default: ""
    font_size: 28
    color: [1.0, 1.0, 1.0, 1.0]
    shadow: true
